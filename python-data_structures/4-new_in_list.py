#!/usr/bin/python3
def new_in_line(my_list, idx, element):
    cp = my_list
    if idx < 0:
        return (cp)
    elif idx > len(my_list)
            return (cp)

    copy[idx] = element
    return (cp)
